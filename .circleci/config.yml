version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/install:
          yarn: true
          executor: cypress/browsers-chrome78-ff70
      - cypress/run:
          name: Chrome
          requires:
            - cypress/install
          record: true
          parallel: true
          parallelism: 2
          browser: chrome
      - cypress/run:
          name: Firefox
          requires:
            - cypress/install
          record: true
          parallel: true
          parallelism: 2
          browser: firefox
